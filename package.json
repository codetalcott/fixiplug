{
  "name": "fixiplug-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/agent-sdk"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --noEmit --watch",
    "build:core": "cd packages/core && bash build.sh",
    "build:agent-sdk": "cd packages/agent-sdk && bash build.sh",
    "test:core": "node test/fixiplug.test.html",
    "test": "echo 'Run individual tests - see CLAUDE.md for details'"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
